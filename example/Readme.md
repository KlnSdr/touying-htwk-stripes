This example uses the (Arimo Nerd Font)[https://github.com/ryanoasis/nerd-fonts/tree/master/patched-fonts/Arimo]. To make it easier to use it is checked into this repo and stored in the `font` directory.

To build it yourself run `make build-example` or `make watch-example` in the repository root.

<!-- todo screenshots -->
